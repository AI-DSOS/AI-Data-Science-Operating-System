---
title: Prompt — Docker Build Failure
purpose: Debug a failing container build.
---

## Prompt
```
This Docker build is failing: {paste Dockerfile + error}. What's the cause,
and does the fix conflict with docs/engineering-standards/docker.md?
```

## Use When
A container build fails locally or in CI.
